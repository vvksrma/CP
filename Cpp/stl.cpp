#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

class Solution {
    public:
        int search(vector<int>& nums, int target) {
            int n = nums.size();
            
            
        }
    };

int main(){
    vector<int> arr = {7, 3, 2, 4, 9, 12, 56};
    int m = 3;

    int res = chocodiff(arr, m);
    cout << res << endl;
    
    return 0;
}